# Copy to .env and fill in. .env is gitignored.
#
# The only required secret. src/llm/client.py raises LLMConfigurationError at
# construction if it is absent -- it does not fall back to a stub, because a stub
# that returns canned text is how a broken deployment passes its own health check.
ANTHROPIC_API_KEY=sk-ant-...
